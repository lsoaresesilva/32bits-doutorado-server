import { InterpretationError } from "./interpretationError";

export default class TypeError extends InterpretationError {
  // TypeError: unsupported operand type(s) for Sub: 'int' and 'str' on line 3

  constructor(traceback: string) {
    super(traceback);
  }
}
