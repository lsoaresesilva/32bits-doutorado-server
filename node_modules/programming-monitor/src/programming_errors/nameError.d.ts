import { InterpretationError } from "./interpretationError";
export default class NameError extends InterpretationError {
    name: string | null;
    constructor(traceback: string);
    static getVariableFunctionName(traceback: string): string | null;
}
