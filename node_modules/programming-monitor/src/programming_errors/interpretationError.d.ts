import { ErrorCategory } from "../enum/errorCategory";
export declare abstract class InterpretationError {
    traceback: string;
    protected constructor(traceback: string);
    line: number;
    category: ErrorCategory;
    static getLine(traceback: string): number;
    static isErro(traceback: string): boolean;
    /**
     * Every error belongs to a category
     * @param traceback
     */
    static getCategory(traceback: string): ErrorCategory;
}
